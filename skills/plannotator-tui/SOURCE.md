# Source

Vendored from `plannotator/herdr-annotate` at commit
`46cbf5dab1746dfeb46eb160c33990b0e0de15b0`:

- `skills/plannotator-tui/SKILL.md`
- `LICENSE`

The SKILL frontmatter adds a pointer to the bundled license. The raw fallback
command from that revision is omitted because this environment installs the
standalone `plannotator-tui` command, matching the referenced dotfiles setup.
