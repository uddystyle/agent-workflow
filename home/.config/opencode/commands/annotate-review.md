---
description: いまの変更を、規約とスペックの2つの観点で別々に見る
---

いまの変更:

!`git diff --stat $ARGUMENTS`

観点の中身は**正本が持つ**。⚠️ ここに写さない——写した瞬間、片方だけ直る場所が増える（D-2）。

@~/.pi/agent/agents/standards.md
@~/.pi/agent/agents/spec.md

上の2つの観点で、**別々に**見る。混ぜない。
⚠️ **どちらが重いかを決めない**——決めるのは人である。

**完了条件**: 観点ごとに見出しが分かれ、各指摘に**根拠**と**確かめたかどうか**が付いている。
0件の観点は、**拠り所を読んだ上で0件**なのか**拠り所が無かった**のかを書いた。
