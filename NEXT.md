# 次にやること

新しいセッションはここから読む。**状態は環境が持つ**ので、ここには引けないものだけ置く。

🔴 **他のプロジェクトの内部事情は書かない**（`DECISIONS.md` D-10）。ここが持つのは道具の次の一手だけである。

## 1. `/agents-md` を本番の repo に当てる

この repo 自身には当て済み（`AGENTS.md` がその出力）。次は、**手で育ってしまった `AGENTS.md`** を相手にする。

```sh
cd <対象の repo> && claude
```

```
/agents-md
```

**見るべきもの**: 生成された `AGENTS.md` そのものではなく、**スキル §5 が出す「落としたもの」の一覧**。
手で育った `AGENTS.md` は、大半が環境から引ける事実で埋まっている。
そのうち**引けない事実（判断・罠）をどれだけ拾えるか**で、この方式が成立するかが決まる。

**受入条件**: §5 の一覧が3分類に割れていて、「引けない事実」に入ったものが
すべて移し先を持っている。

⚠️ **中身をこちらに写して照合しない。** 移し先の `Notes` 節が正本である。
写した時点で、こちらが腐る側になる——実際に一度そうなった（D-10）。

⚠️ **生成物をそのまま `main` に入れない。** 読んで判断する。

## 2. 検査を増やすか決める

いま何本あり、何を確かめるかは `AGENTS.md` の Repository 節が持つ——ここに写さない。
⚠️ **一度写して古くなった。** 受入条件の本数を書いていたが、増えた側に追随せずずれた。

次に候補になるのは、スキルを変えたときの実呼出しを自動化すること。
いまは読んで正しそうでは足りず、実際に呼んで確かめる。手動確認が負担になった時点で決める。

## 3. まだ無いもの

**先に設計を書かず、実害が出たものから1本ずつ足す。**

前に置いていた3行（ガードレール・観点分割・引き継ぎ）は**すべて埋まった**。
何がどの判断で作られたかは `DECISIONS.md` が持つ——ここに写さない。

| 何を                     | いつ作るか                                       |
| ------------------------ | ------------------------------------------------ |
| 既に入ったものを探す     | **実害が出た**。押された履歴に本物が1件あった。⚠️ **自前で書くのは断念した**（下） |
| 誤検知を数える仕組み     | ガードレールに止められて困った回数を、手で数えきれなくなったとき |

**1行目の要点**: ガードレールが見るのは**書き出す瞬間だけ**である。
既に commit されたもの・既に押されたものは、**通り過ぎた後なので見ない**。
入口を塞ぐことと、入ってしまったものを探すことは別の仕事である。

🔴 **自前で書く道は閉じた。** 履歴の全ブロブに表を当てる探査を書いて走らせ、
**13 時間 50 分で 1 件も出力せずに打ち切った**（2026-09-05）。repo 1つ分すら終わっていない。

🔴 **入口の表を監査に流用しない。** 誤りの代償が逆である——
入口は誤検知が書けなくなる痛みを生むので**狭く**、監査は見落としが沈黙を生むので**広く**すべきである。
⚠️ 実際、この表に無い形の鍵（`AQ.` で始まるもの）を1件、**表は素通りさせた**。
狭い表で洗って「無かった」と読むのが、いちばん危ない。

**次に試すこと**: 既製の走査器（`gitleaks` など）を入れて、履歴に当てる。
⚠️ **導入も実行もしていない。効くかは確かめていない。**

⚠️ **この表を設計書にしない。** 空欄のまま置く。

## 4. 測ること

一発通過率を数える。目標と、その数字を選んだ理由は `DECISIONS.md` D-9 が持つ。
素票を10本ほど通したところで数える。

## 5. 観点の名前を付けて pane で立ち上げる（まだ規約にしない）

🔴 **立ち上げ方はここに書かない。** 正本は `skills/worktrees/SKILL.md` §3 が持ち、
名前を観点にする判断は D-1 / D-15 が持つ。⚠️ 一度ここへ写していたが、**写しだった**（D-20）。

ここが持つのは、**まだ決めていないこと**だけである。

`AGENTS.md` の Conventions へ移すのは、**無名で立ち上げて困った回数を数えられるようになってから**。
いまは「そうしてみる」段階である（§3 の方針）。

⚠️ **確かめたのは pi での起動だけ**である。他の種類は試していない。

⚠️ **見えて何が変わるかは、まだ分からない。** 一覧が埋まることは確かめたが、
それで仕事が変わったかは、何本か並べて回すまで言えない。
