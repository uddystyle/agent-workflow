# 次にやること

新しいセッションはここから読む。**状態は環境が持つ**ので、ここには引けないものだけ置く。

🔴 **他のプロジェクトの内部事情は書かない**（`DECISIONS.md` D-10）。ここが持つのは道具の次の一手だけである。

## 1. `/agents-md` を本番の repo に当てる

この repo 自身には当て済み（`AGENTS.md` がその出力）。次は、**手で育ってしまった `AGENTS.md`** を相手にする。

```sh
cd <対象の repo> && claude
```

```
/agents-md
```

**見るべきもの**: 生成された `AGENTS.md` そのものではなく、**スキル §5 が出す「落としたもの」の一覧**。
手で育った `AGENTS.md` は、大半が環境から引ける事実で埋まっている。
そのうち**引けない事実（判断・罠）をどれだけ拾えるか**で、この方式が成立するかが決まる。

**受入条件**: §5 の一覧が3分類に割れていて、「引けない事実」に入ったものが
すべて移し先を持っている。

⚠️ **中身をこちらに写して照合しない。** 移し先の `Notes` 節が正本である。
写した時点で、こちらが腐る側になる——実際に一度そうなった（D-10）。

⚠️ **生成物をそのまま `main` に入れない。** 読んで判断する。

## 2. 検査を増やすか決める

`install.sh` の9つの受入条件は `./tests/install.sh` が隔離して確かめる。
何を確かめるかは `AGENTS.md` の Repository 節が持つ——ここに写さない。写した側が先に古くなる。

次に候補になるのは、スキルを変えたときの実呼出しを自動化すること。
いまは読んで正しそうでは足りず、実際に呼んで確かめる。手動確認が負担になった時点で決める。

## 3. まだ無いもの

**先に設計を書かず、実害が出たものから1本ずつ足す。**

| 何を                         | いつ作るか                             |
| ---------------------------- | -------------------------------------- |
| ガードレール（`PreToolUse`） | エージェントが実際に危ないことをした後 |
| レビューの観点分割           | レビューが1周で終わらなくなったとき    |
| 引き継ぎのスキル             | セッションを跨ぐ必要が出たとき         |

⚠️ **この表を設計書にしない。** 空欄のまま置く。

## 4. 測ること

一発通過率を数える。目標と、その数字を選んだ理由は `DECISIONS.md` D-9 が持つ。
素票を10本ほど通したところで数える。

## 5. エージェントを herdr の pane で起動するか決める

herdr の観測機能（注意順の一覧・状態の記号・通知）は、**pane の前景プロセス**として
走っているエージェントにしか効かない。デーモンが抱える PTY で動かしている限り、
設定を入れても永久に空のままである（`DECISIONS.md` D-7）。

**確かめ済み**: pane の中で立ち上げれば検出され、状態も取れる。
2本並べたら**見分けがつかなくなり**、タブに観点を書いたら別物になった（D-1）。
効かない可能性は消えた。

**決めること**: 起動の規約として、**観点の名前を付けて pane で立ち上げる**を採るか。

herdr の起動口は、名前・種類・置き場所を一度に受け取る。
つまり「pane で立ち上げる」と「観点で名乗らせる」は**別々の習慣ではなく、1つの動作**である。
種類も選べるので、**同じ観点を別のモデルに見せる**ことも同じ入口でできる（D-8 の再検討条件）。

⚠️ **決める材料は、まだ「見えるようになる」だけである。** 見えて何が変わるかは、
何本か並べて動かすまで分からない。**先に規約を書かず、しばらくそうしてみる**のが安い。

⚠️ **手で立ち上げた分は無名のまま並ぶ。** いま2本がその状態にある。
規約にするなら、そこも揃える必要がある。
