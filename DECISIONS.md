# 判断

結論と、**選ばなかった理由**だけを置く。引ける事実は書かない。

初回はすべて 2026-09-04 の本人判断。

---

## D-1 並列は「観点」で分ける

エージェントの種類（local claude / cloud claude / codex / pi）で役割を決めるのをやめる。

**理由**: 種類で分けると、同じ種類が2つ動いた瞬間に識別できなくなる。
実際に、同じ種類のエージェントが2つ動いた結果、通知が誰にも届かなくなったことがある。
契約は書けていたが、**契約が識別できないものを契約していた**。

**代わりに**: 観点（何を見るか）ごとにプロンプトを分け、サブエージェントで context を分ける。
「同じエージェントが実装とレビューを兼ねると盲点で見る」は正しいが、
**その解は人格の分離ではなく context の分離である。**

## D-2 文書は「引けない事実」だけを持つ

行番号・キー名・テスト本数・実測値を文書に書かない。**引くコマンドを書く。**

**理由**: 2026-09-04 の1セッションで、事実の誤認が6件起きた。
6件とも「1コマンドで引ける事実を、文書から読んで信じた」形だった。
うち1件は本番で常に失敗する実装まで到達し、
**検査も同じ誤りを埋め込んでいたため緑のまま通った。**

**書いてよいもの**: 判断の理由、選ばなかった案、罠の存在、規約。
**書いてはいけないもの**: 環境を見れば分かること。

**腐りはじめる時点**: 引ける事実は、**読んだ瞬間から古くなる**。
別のセッションが `install.sh` のある振る舞いを読んで報告したとき、その振る舞いは
報告が届く前にコードで塞がれていた。読んだ時点では正しく、報告した時点では古い。
**文書に写すまでもなく、口に出した時点でずれている。**
だから「こまめに書き直す」では届かない。写す場所を減らすのではなく、写すのをやめる。

## D-3 道具はマシンに置く。プロジェクトには置かない

スキルもガードレールも `~/.claude/` 配下に置き、dotfiles で管理する。

**理由**: プロジェクトごとに仕組みを入れると、プロジェクトの数だけ腐る。
マシンに1つ置けば、全プロジェクトに同時に効く。

**例外**: 各 repo の `AGENTS.md` 1枚。ただし生成物であり、日付とコミットの印を持つ。

## D-4 `AGENTS.md` は生成物にする

手で維持しない。repo を見て毎回作り直す。冒頭に生成日時と対象コミットを刻む。

**理由**: 腐った `AGENTS.md` は、無いより悪い。
手で育てた `AGENTS.md` が読む気を失う大きさまで膨れ、いつの事実か誰にも分からなくなった例を見ている。

**目安**: 5KB 前後。モデルの context に毎回載るので、短く高信号に保つ。

## D-5 ガードレールは実行の瞬間に効かせる

受入時の門ではなく、`PreToolUse` フックで止める。

**理由**: 門は受入まで気づけない。フックは危険な操作そのものを止める。
止めるときは**次の一手を必ず書く**——何をすれば進めるかが分からないと、
エージェントは別の壊し方を探す。

## D-6 禁止ではなく、やることを書く

「〜しない」ではなく「〜する」で書く。

**理由**: 禁止は禁止対象を context に引き込み、かえって出やすくする。
どうしても禁止が要るのは、肯定形で言い換えられない硬い制約だけ。

## D-7 pi / herdr / queue を使わない

配車・台帳・門の機構を持たない。Ghostty のタブで `claude` を立ち上げるだけにする。

**理由**: 配車は 2026-09-02 に退役し、以後は使われていなかった。
台帳（進行状態を複数のファイルに写したもの）は D-2 の意味で全体が腐敗源だった。
機構が増えるほど、機構自体の保守が仕事を食う。

**代償**: 「配ったのに戻ってこない」を機械が検知しなくなる。
人が見る。それで足りるかは使ってみて決める。

**その後**: この代償は herdr 側が埋めていた。エージェント一覧を注意が要る順に並べ、
blocked / working / done / idle を区別し、finished と needs-input を通知する機能が
最初から入っている。**捨てた配車が持っていた仕事は、道具の側に既にあった。**
だから配車を作り直す必要はない——設定を書くだけでよい（D-11）。

**射程**: 止めるのは**ワークフロー**である。マシンに入っている統合そのものは対象外。
herdr を道具として使うこと、その設定をこの repo で持つことも射程外である。
🔴 **戻さないのは配車・台帳・門の3つだけ**であって、道具そのものではない。
`~/.claude/settings.json` の `SessionStart` は herdr の状態報告フックを呼ぶが、
これは `HERDR_ENV=1` のときだけ動き、herdr を立ち上げなければ何もしない。
🔴 **消さない**——herdr が管理するファイルで、再インストールで戻る。
⚠️ **`settings.json` には別管理のフックが同居しているので、手で編集しない。**

## D-8 OpenCode はいまは足さない

**理由**: スキル機構もフック機構も Claude Code に既にある。
いま足すと agent の種類が増え、D-1 で捨てた軸が復活する。

**再検討する条件**: プロバイダを乗り換えたくなったとき。
OpenCode は 75+ の LLM を1つの config で扱えるので、
乗り換えがワークフローの作り直しにならない。

## D-9 成功は「一発で通る率」で測る

**理由**: 旧ワークフローの実績は approved 49% / changes_requested 33% / blocked 18%。
半分は一発で通らなかった。この率が上がらなければ、新しい仕組みも失敗である。

**目標**: 75%。

## D-10 この repo は公開されても成立する形にする

道具と判断だけを持つ。**他のプロジェクトの内部事情は持たない。**

**理由**: 道具と判断は、他人が読んでも効く。公開できない状態は、共有する・引用する・
外からレビューを受けるという選択肢を、使う前に閉じてしまう。

**持たないもの**: 他プロジェクトのドメイン事実、スキーマ、セキュリティ設定の状態、
未対処の弱点、ブランチ名、票番号。それらは**そのプロジェクトの `AGENTS.md` の
`Notes` 節**が正本である（D-3 の例外1枚がここで効く）。

判断の理由として過去の事象を引くときは、**何が起きたかの形**だけを書く。
どこで起きたかの固有名と、その系のいまの状態は書かない。
形は他人にも効き、固有名と状態は、公開できないうえ、その系が変われば腐る（D-2）。

**受入条件は例外にならない**: 一度、判定材料のつもりで別プロジェクトのドメイン事実を
ここに写していた。判定に中身は要らず、写す必要も無かった。
そして写した側は、あちらが直っても直らない——D-2 が言う形がそのまま起きる。

🔴 **公開は履歴も公開する。** 作業ツリーから消しても、コミットに残ったものは読める。
持ち込んだと気づいたら、**押す前に履歴ごと直す**。押した後では取り消せない。

**やったこと**: 公開の直前に履歴を作り直し、初期コミット1本にした。
作業ツリーを綺麗にした時点では、**削除した内容がまだ差分から読めた**。
一度も押していなかったので、その時点なら無傷でやり直せた。
**押した後には戻れない**——だから公開の判断は、最初のコミットより前に済ませる。

**代償**: 判断の理由から具体が減り、読み手にとっての説得力は落ちる。
それでも形が残れば判断は再現できる。固有名は再現に要らない。
粒度の細かいコミットメッセージも失うが、判断はこのファイルが持つ。失うのは経緯だけである。

## D-11 マシンの設定は stow で配る

`~/.config/` に置く設定は、この repo が正本を持ち、GNU Stow で `~` へ張る。

**理由**: 設定は「引けない事実」ではないが、**手で入れると入れた場所ごとにずれる**。
repo を正本にすれば、変える場所が1つになる。

**選ばなかった案**: `install.sh` にファイル単位の symlink を書き足す。1ファイルなら足りるが、
設定が増えるたびに設置の手続きを書くことになる。stow は木の形だけで済む。**形が仕様になる。**

🔴 **ディレクトリごと張らない**（`--no-folding`）。張り先が無いとき、stow は
ディレクトリを1本の symlink に畳む。`~/.config/herdr/` にはソケットとログが同居するので、
畳むと repo の中にランタイムが作られる。

**確かめたこと**: 道具が設定を書き換えても **symlink は壊れず、書き込みは repo に届く**
（`herdr config reset-keys` で実測）。⚠️ **これは利点である**——設定画面から変えた分が
`git status` に出る。バックアップは張り先に落ちるので repo は汚れない。

**なぜスキルまで stow にしないか**: stow は**畳む高さを張り先の既存構造で決める**。
`~/.claude/skills/` が在るマシンではスキル1つ分を畳むが、`~/.claude` ごと無いマシンでは
**`~/.claude` 自体を repo への symlink にする**（実測）。設定も hooks も別管理のスキルも巻き込む。
`--no-folding` で防ぐと、今度はスキルがファイル単位になり、repo にファイルを足しても効かない。
**スキルは畳んでほしく、設定は畳んでほしくない**——要求が逆である。
`install.sh` は `mkdir -p` してから葉だけを張るので、**結果が張り先の状態に左右されない**。
役目は symlink を張ることではなく、**畳む高さを固定し、止めたときに次の一手を出すこと**である。

**代償**: `brew install stow` が前提に増える。無ければ `install.sh` は止まり、入れ方を出す。
